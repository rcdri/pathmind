package com.pathmind.ai;

/** Stable structural concepts used to index curated graph examples. */
public enum AiExampleTrait {
    CONDITION_WITH_SENSOR,
    IF_ELSE,
    REPEAT,
    REPEAT_UNTIL,
    FORK_JOIN,
    PARAMETER_ATTACHMENTS,
    VARIABLES,
    LISTS,
    NESTED_CONTROLS,
    ROUTINE_ARGUMENTS,
    INVENTORY_WORKFLOW,
    NAVIGATION_COLLECTION
}
